{% extends 'layout.html.twig' %}

{% block body %}

  <h1>Fee creation</h1>

  {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-primary" />
  {{ form_end(form) }}

  <a href="{{ path('fee_index') }}" class="btn btn-primary">Back to the list</a>

{% endblock %}